[project]
name = "load-tester"
version = "0.1.0"
description = ""
authors = [
    {name = "darmoise",email = "darmoise@localhost"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "hy (>=1.0.0,<2.0.0)",
    "kafka-python (>=2.2.2,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
